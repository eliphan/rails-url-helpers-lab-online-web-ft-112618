<div>
  <% @students.each do |student| %>
    <div><%= student_name = student.first_name + " " + student.last_name %></div>
    <div>link_to student_name, student_path(student)</div>
  <% end %>
</div>