<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
    binding.pry
    <div><%= link_to student_name, students_path(students.id) %></div>
  <% end %>
</div>